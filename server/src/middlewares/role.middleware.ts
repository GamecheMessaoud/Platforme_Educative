/**
 * Re-exports roleMiddleware from auth.middleware to avoid duplication.
 * Use AuthRequest and roleMiddleware from '../middlewares/auth.middleware' directly.
 */
export { roleMiddleware } from './auth.middleware';
